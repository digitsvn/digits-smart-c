#!/usr/bin/expect -f
set timeout 10
set password "VTKtvEW@z31q"
set host "root@116.118.2.38"

spawn ssh $host "cd /root/smartc-cloud && npx pm2 logs smartc-cloud --lines 100 --nostream"
expect {
    "password:" { send "$password\r" }
}
expect eof
