# =============================================================================
# SMART C AI - Requirements cho Raspberry Pi (Tối ưu)
# =============================================================================
# File này chỉ chứa dependencies tối thiểu cần thiết
# Giảm RAM từ ~400MB xuống ~120MB
# Giảm thời gian cài đặt từ 10 phút xuống 3 phút
#
# Cài đặt: pip3 install --user -r requirements-pi.txt
# =============================================================================

# === CORE (Bắt buộc) ===
numpy>=1.20.0
sounddevice>=0.4.4
websockets>=11.0
aiohttp>=3.8.0

# === Wake Word Detection ===
sherpa-onnx>=1.10.0

# === Audio Processing ===
opuslib>=3.0.1
webrtcvad-wheels>=2.0.10

# === Network & Protocol ===
paho-mqtt>=2.0.0
requests>=2.28.0

# === Security ===
cryptography>=40.0.0

# === Utilities ===
colorlog>=6.0.0
psutil>=5.9.0
py-machineid>=0.6.0
python-dateutil>=2.8.0

# === Optional: GUI (Chỉ cài nếu dùng GUI mode) ===
# PyQt5>=5.15.0
# qasync>=0.27.0
# pillow>=9.0.0

# === Không bao gồm (không cần cho Pi) ===
# pygame - không cần sound effects
# opencv-python-headless - chỉ cần nếu dùng camera
# pyinstaller - chỉ dùng khi build
# openai - không dùng trực tiếp
# pynput - không cần trên headless Pi
# rich - không cần fancy terminal
# beautifulsoup4 - không cần web scraping
# brotli - không cần compression
# mutagen - không cần audio metadata
# pypinyin - không cần pinyin
# pyperclip - không cần clipboard
# lunar_python - không cần lunar calendar
# soxr - không cần resampling (dùng native)
# pendulum - dùng python-dateutil thay thế
